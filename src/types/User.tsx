export interface UserData {
    given_name: string;
    family_name: string;
    name: string;
    email: string;
    token: string;
}

export interface User {
    id: number;
    fullname: string;
    email: string;
}